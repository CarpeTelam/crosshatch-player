---
topic: crosshatch-player v1 game platform: scripted games, seat-based multiplayer, install by file drop on x4pro and sticky
updated: 2026-09-26T14:47
---

- (event) Create started (interactive, express mode by user choice); inputs: brief.md + addendum.md (brief-crosshatch-player-2026-09-26) and ARCHITECTURE-SPINE.md + game-api-seed.md (architecture-crosshatch-player-2026-09-26)
- (decision) Slug crosshatch-player inherited from the brief and architecture folders
- (decision) Brief and addendum are sources (fully absorbed); ARCHITECTURE-SPINE.md and game-api-seed.md are adopted companions because downstream builds from their AD rules and API contract; AGENTS.md adopted for project conventions
- (decision) Where architecture and brief disagree, architecture wins: it is the later, user-confirmed decision-of-record
- (decision) Superseded brief claims: script decides turn order via status() and runtime enforces it (brief said runtime owns turn order), AD-8
- (decision) Superseded brief claims: whole-frame refresh with fast/half/full hints and 4-level dithered fills; dirty-region refresh, true grayscale, image scaling deferred
- (decision) Superseded brief claims: v1 install only via SD inbox /games/*.cpgame (web file manager or USB); web Games page deferred, /api/games* reserved
- (decision) Superseded brief claims: engine is Lua 5.5.1 unmodified (spike chose Lua over Berry; 5.4.9 fallback only on measured regression); brief's 'Lua 5.4 pending Berry spike' is resolved
- (decision) Superseded brief claims: upstream-touch cap set to the 9-file ledger plus 1 reserve, CI-enforced (brief left the cap to architecture)
- (capability) CAP-1 author a turn-based game as a script against a documented, versioned host API (touch, drawing, refresh hints, timer, per-game store); success = 3 first-party games contain no game-specific firmware code
- (capability) CAP-2 faulty or runaway scripts cannot crash, hang, or corrupt the device; each failure ends the session in an error view with Back
- (capability) CAP-3 install a game by dropping a .cpgame on the SD card; validated and listed without reboot or firmware build; invalid packages rejected with a reason; reinstall keeps saves
- (capability) CAP-4 games launcher reachable from Home; start solo/pass in <= 3 taps, no text entry, paged, Continue for saves, remove game
- (capability) CAP-5 one script plays solo, pass-and-play, and Play Nearby (2 devices) with no radio code in the script
- (capability) CAP-6 hidden-information pass games never leak the other seat's view, including via ghosting, sleep, or resume
- (capability) CAP-7 match lifecycle: pause, leave, play again, resume interrupted solo/pass match, clean end when a Nearby peer leaves or goes silent
- (capability) CAP-8 shared icon library as the design language for games and runtime screens; games may also ship own 1-bit images
- (capability) CAP-9 self-contained API docs (reference + LuaLS stub + icon catalog) an AI assistant can write a working game from; movable to the starter repo unchanged
- (capability) CAP-10 at least three first-party games ship as .cpgame release assets: solo puzzle, open-information 2P, hidden-information 2P; together they exercise every mode
- (capability) CAP-11 upstream develop merges touch no game code; upstream-file changes capped by a ledger and enforced by a fork-only CI job
- (constraint) Games code active only on x4pro/sticky (+gh_release variants, simulator) under FREEINK_CAP_GAMES; game libs compile unreferenced for default, x4c, papermono at zero C3 cost
- (constraint) Upstream touches limited to the AD-3 ledger (9 files) + 1 reserve; exceeding needs an architecture update; never move freeink-sdk
- (constraint) Game rules live only in scripts; turn/sync/protocol logic only in host-testable GameCore; no game-specific C++
- (constraint) Scripts reach the host only through ch.*; no file, radio, or raw framebuffer access; text chunks only; 2M-instruction per-callback budget; 256 KB PSRAM heap
- (constraint) Codec limits enforced in every mode: snapshot <= 1,400 B (one ESP-NOW v2 payload), move <= 256 B, ch.store <= 4 KB
- (constraint) Scope: simple turn-structured games only; API additions must serve them; no frame loop, drag, sprites, sound, or timers under 1 s
- (constraint) API versioned by integer level, additive only within a level; icon names are part of the level
- (constraint) Play Nearby cannot run while the web server or any Wi-Fi activity is up; radio unencrypted, cooperative room assumed
- (constraint) Budgets: runtime adds <= 250 KB flash to x4pro image; internal RAM limited to 16 KB VM + 4 KB link stacks + Wi-Fi driver; rest in PSRAM; lobby refuses below 100 KB free internal heap
- (constraint) No v1 flow needs text entry; runtime text via tr(STR_GAMES_*)
- (note) Demand beyond the author is unknown and accepted for a passion project; not a gate
- (decision) Candidate first-party game list and category coverage go to spec-authored companion first-party-games.md; shared terms go to glossary.md
- (assumption) CAP-9 success tested as: an AI given only the API docs writes a game that installs and plays; inferred from brief's 'documented well enough for an AI to write against'
- (assumption) Restaurant test is demonstrated by observing a child on a real device, not automated
- (question) ESP-NOW reliability, battery cost per match, and Sticky / mixed X4 Pro-Sticky behaviour are unmeasured; radio epic measures before tuning 400 ms / 10 s / 100 KB
- (question) Does internal heap recover after ESP-NOW teardown, or does leaving a Nearby match need silentRestart()?
- (question) Lua 5.5.1 spike re-run with worst-case C-stack test must pass before API level 1 freezes; regression reverts to 5.4.9
- (question) Exact v1 icon list is open; fixed in the API docs epic
- (question) Which three titles ship as first-party games is open; chosen in the games epic
- (event) SPEC.md, glossary.md, first-party-games.md rendered from memlog: 11 capabilities, 10 constraints, 11 non-goals, 3 assumptions, 5 open questions
- (event) Pass 1 coherence: PASS; every CAP has intent+success, constraints each rule out options, non-goals explicit, success signal demonstrable, IDs CAP-1..11 unique; CAP-11 intent names the ledger mechanism because the brief requires a cap, accepted
- (event) Pass 2 preservation: PASS; brief/addendum claims land in kernel, companions, or adopted architecture (plugin finding -> Why; runtime/radio starting points -> AD-1/5/6/13/18; candidate games -> first-party-games.md; starter-repo and >2-seat vision -> Non-goals + CAP-9); superseded brief claims logged as decisions
- (event) Wrapper-only content dropped: executive-summary prose, 'What Makes This Different' positioning, research citation numbers [n], brief polish/finalize process notes
- (direction by user) Only one device on hand; more are ordered. Two-device radio testing waits for them and blocks no other development; radio logic proceeds against FakeLink host tests
- (decision by user) Radio reliability / battery / Sticky-mixed question stays open but is non-blocking; answered when the second device arrives, before tuning 400 ms / 10 s / 100 KB
- (decision by user) Assume Lua 5.5.1 works as well as or better than the spiked 5.4.7; the spike re-run no longer gates the API level 1 freeze; go straight to implementation and fix 5.5.1 problems if they surface (5.4.9 stays the fallback). Supersedes AD-4's pre-freeze re-run
- (note) Heap recovery after ESP-NOW teardown can be measured on a single device (init/teardown without a peer); stays open, non-blocking
- (event) SPEC.md re-rendered: Lua 5.5.1 moved from Open Questions to Assumptions; radio question marked waiting on second device, non-blocking; heap question marked single-device measurable. Coherence and preservation re-checked: PASS
- (decision by user) User confirmed all assumptions: CAP-9 AI-from-docs test, restaurant test by observing a child on a real device, architecture wins over brief; Assumptions section retired, restaurant-test method folded into Success signal
- (event) Architecture spine AD-4 amended via bmad-architecture update to match the Lua 5.5.1 decision; spec and spine agree
- (note) First-party fit notes: Hangman in pass needs the setter to enter a word, conflicting with no-text-entry unless the word comes from a built-in list; Battleship boards must be packed (e.g. strings) to stay well under 1,400 B
- (decision by user) First-party games chosen: Sudoku (solo puzzle), Ultimate tic-tac-toe (open-information 2P; the crosshatch namesake), Battleship (hidden-information 2P); resolves the titles open question
- (note) Game fit notes: Sudoku uses a tap number picker (no text entry) and ships a puzzle bank rather than generating unique-solution puzzles inside the 2M-instruction budget; Ultimate tic-tac-toe's 9x9 grid gives ~50 px cells on the 480 px X4 Pro width, small for kids, so the active board needs an enlarged view; Battleship needs tap-to-place with rotate or random placement (no drag) and packed boards
- (event) Re-rendered: CAP-10 success names the three games; first-party-games.md rewritten with picks and fit notes; titles open question removed; coherence and preservation re-checked: PASS
